
// Solo retornara un solo id, por eso no se deben repetir

const formulario = document.getElementById('formulario');
console.log(formulario);

const footer = document.getElementById('footer');
console.log(footer);